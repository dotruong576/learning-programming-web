{
  "devDependencies": {
    "husky": "^9.0.11"
  },
  "scripts": {
    "postinstall": "(cd backend && npm install) && (cd ../frontend && npm install) && (cd ../frontend-admin npm install)",
    "prepare": "husky install"
  }
}
